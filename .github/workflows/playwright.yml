name: Playwright CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  saucedemo-tests:
    name: SauceDemo UI Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium

      - name: Run SauceDemo Tests
        run: |
          mkdir -p allure-results/sauceDemo blob-report/sauceDemo
          ALLURE_RESULTS=allure-results/sauceDemo BLOB_REPORT=blob-report/sauceDemo npx playwright test --project=sauceDemo
        env:
          CI: true
          BASE_URL: ${{ secrets.BASE_URL }}

      - name: Upload SauceDemo Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sauceDemo-reports
          path: |
            allure-results/sauceDemo
            blob-report/sauceDemo
            test-results/

  fakestore-tests:
    name: FakeStore API Tests
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run FakeStore Tests
        run: |
          mkdir -p allure-results/fakeStoreAPI blob-report/fakeStoreAPI
          ALLURE_RESULTS=allure-results/fakeStoreAPI BLOB_REPORT=blob-report/fakeStoreAPI npx playwright test --project=fakeStoreAPI
        env:
          CI: true
          API_USERNAME: ${{ secrets.API_USERNAME }}
          API_PASSWORD: ${{ secrets.API_PASSWORD }}

      - name: Upload FakeStore Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fakestore-reports
          path: |
            allure-results/fakeStoreAPI
            blob-report/fakeStoreAPI
            test-results/

  merge-reports:
    name: Merge Playwright + Allure Reports
    runs-on: ubuntu-latest
    needs:
      - saucedemo-tests
      - fakestore-tests
    if: always()

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Merge Blob Reports
        run: |
          mkdir -p all-blob-reports
          cp -r blob-report/sauceDemo/* all-blob-reports/
          cp -r blob-report/fakeStoreAPI/* all-blob-reports/
          npx playwright merge-reports --reporter html ./all-blob-reports

      - name: Merge Allure Results
        run: |
          mkdir -p allure-results
          cp -r allure-results/sauceDemo/* ./allure-results/
          cp -r allure-results/fakeStoreAPI/* ./allure-results/

      - name: Install Allure
        run: npm install -g allure-commandline

      - name: Generate Allure Report
        run: allure generate ./allure-results -o ./allure-report --clean

      - name: Upload Final HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: playwright-html-report
          path: playwright-report/
          retention-days: 14

      - name: Upload Final Allure Report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report/
          retention-days: 30
